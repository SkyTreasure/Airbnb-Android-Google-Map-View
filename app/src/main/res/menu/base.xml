<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_lite_mode"
        android:title="Lite Mode in RecyclerView"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_home"
        android:title="Map and Viewpager"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_search"
        android:title="Search Places"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_show_direction"
        android:title="Show Direction"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_moving_marker"
        android:title="Moving Markers on Map"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_list_view"
        android:title="List View"
        app:showAsAction="never" />


</menu>